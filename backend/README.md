# Romifleur Backend API

FastAPI backend for Romifleur ROM downloader.

## Setup

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

## API Docs

Once running, open: http://localhost:8000/docs
